name: Tests

on:
  push:
    branches: [main]

jobs:
  bible-domain:
    runs-on: xcode-27
    defaults:
      run:
        working-directory: Modules/BibleDomain
    steps:
      - uses: actions/checkout@v5
      - name: Run BibleDomain tests
        run: swift test
